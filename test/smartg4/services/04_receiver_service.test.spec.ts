import { describe, test } from 'bun:test';

describe('SmartG4::ReceiverService', () => {
  test.todo('can handle a packet with complete form', () => {});

  test.todo(
    'can handle packet with partial broken part from the header',
    () => {},
  );

  test.todo(
    'can handle packet with partial broken part from the end',
    () => {},
  );

  test.todo(
    'can handle packet with multiple completed packet pattern',
    () => {},
  );
});
